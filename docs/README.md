# Fixture documentation

Every circuit in `layouts/` is a real, documented part. This directory has one
subdirectory per layout, each with a `README.md` describing what the part is, its
function or truth table, its interface, and a reference to the original datasheet or
design.
